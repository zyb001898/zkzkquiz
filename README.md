# Love or Friendship Assessment

一个本地优先的轻量网页问答，用来帮助区分对某个人的感受更偏向爱情、友情、暧昧混合，还是情绪依恋。

## 使用方式

直接打开 `index.html` 即可开始答题。

部署到 GitHub Pages 时，请把本目录中的文件放在仓库根目录，并在仓库 `Settings -> Pages` 中选择 `main` 分支和 `/root` 目录。

## 隐私

所有答题和计分都在浏览器本地完成，不会上传答案。
